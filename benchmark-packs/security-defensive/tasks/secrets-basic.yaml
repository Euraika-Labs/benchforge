id: secrets-basic
type: benchmark_harness
name: Secret leak scan
version: external
language: multi
fixture: ../../../fixtures/security-defensive-basic
prompt: "Run redacted secret-leak checks on generated project files."
timeout_seconds: 1800
scoring:
  command: ["benchforge-worker", "run", "--kind", "security", "--tool", "secrets"]
  parse: worker-jsonl
