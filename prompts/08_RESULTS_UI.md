# Codex prompt: results UI

Build the result UX.

Scope:

- run builder screen;
- live run queue;
- comparison table;
- filters;
- run detail view;
- diff viewer;
- stdout/stderr viewer;
- test output viewer;
- reproducibility metadata view;
- JSONL/CSV/Markdown export.

Acceptance:

- user can run quick-smoke benchmark and inspect every artifact;
- export contains enough metadata to reproduce the run.
